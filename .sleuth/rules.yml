rules:
  - label-if-big:
      description: "Label if big"
      conditions:
        - magnitude='Small'
      actions:
        - add_deploy_tag: 'smallish'
  - comment:
      conditions:
        - future_deploy=false
        - environment='Production'
      actions:
        - notify_mentioned_issues

 
 
 
 
 
 
 
 
 
 
